---
import { getCollection } from "astro:content";
import Baz from "../layouts/Baz.astro";

const allBits = await getCollection("thoughts");
const pageTitle = "library";
---

<Baz pageTitle={pageTitle}>
    <p>Library</p>
</Baz>
